spring:
  cloud:
    config:
      enabled: false
  application:
    name: blog-backend
  data:
    mongodb:
      database: test
      port: 0
      host: localhost
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.28

